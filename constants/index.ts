import { Colors } from './colors';
import { VyraDesignSystem } from './designSystem';
import { Radius, Spacing } from './theme';

export const Sizes = {
  spacing: Spacing[4],
  radius: Radius.md,
} as const;

export { Colors };
export { VyraDesignSystem };
